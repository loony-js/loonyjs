{
  "name": "@loonyjs/core",
  "version": "0.1.0",
  "description": "LoonyJS core framework — DI, modules, HTTP, decorators",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:watch": "tsc -p tsconfig.json --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "express": "^4.19.2",
    "reflect-metadata": "^0.2.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "typescript": "^5.4.5"
  },
  "peerDependencies": {
    "reflect-metadata": "^0.2.2"
  }
}
