{
  "name": "@loonyjs/typeorm",
  "version": "0.1.0",
  "description": "TypeORM integration for LoonyJS — DataSource, repositories, @InjectRepository",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:watch": "tsc -p tsconfig.json --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@loonyjs/core": "*",
    "typeorm": "^0.3.20"
  },
  "peerDependencies": {
    "reflect-metadata": "^0.2.2"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
